spring:
  profiles:
    active: local
  application:
    name: codepulse-backend
  jackson:
    default-property-inclusion: non_null
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate     # Flyway owns the schema, Hibernate never auto-generates
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  error:
    include-message: always

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.codepulse: DEBUG
    org.flywaydb: DEBUG

jwt:
  access-token:
    secret: ${JWT_ACCESS_SECRET:dGhpcy1pcy1hLXZlcnktc2VjdXJlLWxvbmctc2VjcmV0LWtleS1mb3ItYWNjZXNzLXRva2Vu}
    expiry-seconds: 900
  refresh-token:
    secret: ${JWT_REFRESH_SECRET:dGhpcy1pcy1hLXZlcnktc2VjdXJlLWxvbmctc2VjcmV0LWtleS1mb3ItcmVmcmVzaC10b2tlbg==}
    expiry-seconds: 604800       # 7 days

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}